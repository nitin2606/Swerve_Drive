ament_uncrustify --reformat test/tutorial_test.cpp

✅ Run All Linters Automatically
colcon test --packages-select <your_package> --event-handlers console_cohesion+

✅ Run Specific Linters


# with verbose output
colcon test --packages-select swerve_drive_controller --ctest-args -R uncrustify --output-on-failure


Cppcheck
colcon test --packages-select <your_package> --ctest-args -R cppcheck

Uncrustify
colcon test --packages-select <your_package> --ctest-args -R uncrustify

CPPLint
colcon test --packages-select <your_package> --ctest-args -R cpplint

ament_lint_cmake
colcon test --packages-select <your_package> --ctest-args -R cmakelint

ament_pep257 / flake8 (for Python code)
colcon test --packages-select <your_package> --ctest-args -R pep257
colcon test --packages-select <your_package> --ctest-args -R flake8


🧪 Unit & Integration Testing

✅ Run All Tests
colcon test --packages-select <your_package>

✅ Run Tests and Show Output
colcon test --packages-select <your_package> --event-handlers console_direct+


✅ Test a Specific Target
colcon test --packages-select <your_package> --ctest-args -R <regex_for_test_name>


✅ Re-run Only Failed Tests
colcon test --packages-select <your_package> --continue-on-error
colcon test-result --all


✅ Show Summary of Test Results
colcon test-result

✅ Detailed Results for All Packages
colcon test-result --all

### CHECK ###
Problem: Your C++ code has style issues based on Google's C++ style guide.

ament_cpplint .
Then, fix the reported issues (e.g., line length, naming, spacing).

Tip: Avoid using namespace std;, keep lines ≤ 100 characters, use consistent naming.



Problem: Your CMakeLists.txt or other .cmake files have syntax or style issues.

ament_uncrustify --reformat path/to/your/file.cpp
or
ament_uncrustify --reformat   (formats all files)


ament_lint_cmake .
Follow its output to correct issues like spacing, indentation, or bad practices.


lsblk
sudo mount /dev/sda1 /mnt/usb


commit hash: 5839bae8
